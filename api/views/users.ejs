<% if(role){ %>
<% if(users){ %>
    
    <div class="container mt-5 text-center">
        <h3>Table de reception</h3>
    </div>
    <div class="container mt-5">
    <div class="card mt-5 shadow p-3 mb-5 bg-body rounded">
    <table class="table">
        <thead>
          <tr>
            <th scope="col">Username</th>
            <th scope="col">Email</th>
            <th scope="col">Action</th>
          </tr>
        </thead>
        <tbody>
            <% users.map(res=>{ %>
          <tr>
            <th scope="row"><%= res.username %></th>
            <td><%= res.email %></td>
            <td><form action="/users/<%= res._id %>" method="POST"><button class="btn btn-danger"  onclick="return confirm('Are you sure you want to delete this record?')" type="submit">Delete</button></form></td>
        </tr>
        <% })}else{ %>
        </tbody>
      </table>
    </div>
    </div>
    
        <p>no user found!</p>
    <% } %>
<% }else{ %>
    <script>
        location.replace("/")
    </script>
  <% } %>



 